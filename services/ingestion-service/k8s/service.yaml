apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: convoy
  labels:
    app: ingestion-service
spec:
  selector:
    app: ingestion-service
  ports:
    - port: 8080
      targetPort: 8080
